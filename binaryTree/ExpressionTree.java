package binaryTree;

//import java.util.Stack;

public class ExpressionTree {
	/*static char evaluate(int a,int b,char op) {
		int result = 0;
		switch(op) {
		case '+':
			result = a + b;
			break;
		case '-':
			result = a - b;
			break;
		case '*':
			result = a * b;
			break;
		case '/':
			result = a / b;
			break;
		}
		return (char)(result + 46);
	}
	static BinTree expressionTree(String exp) {
		Stack<Character> op = new Stack<>();
		BinTree tree = new BinTree(0);
		int n = exp.length();
		for(int i=1;i<n;i++) {
			if(exp.charAt(i) >= '0' && exp.charAt(i) <= '9') op.push(exp.charAt(i));
			else {
				
			}
		}
	}
	public static void main(String[] args) {
		
	}*/
}
